#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main - print random number and say positve, negative or zero
 * Return:0
 */
int main(void)
{
char az;
for (az = 'a'; az <= 'z'; az++)
putchar(az);
putchar('\n');
return (0);
}
